#include<stdio.h>
#include <stdlib.h>

int main()
{
    char a[100],f[100],g=0;
    int b,e=0;
    scanf("%s%d",&a,&b);
    int c=strlen(a);
    int d=c-b;
    for(int i=0; i<c; i++)
    {
        if(e%2==0 && i<2*b)
        {
            printf("%c",a[i]);
        }
        else
        {
            f[g]=a[i];
            g++;
        }
        e++;
    }
    printf("\n");
    for(g=0; g<d; g++)
    {
        printf("%c",f[g]);
    }
    
}



Question:

Two string values S1 and S2 are interlaced and passed as a single input string S. Given L1 which is the length of S1, print S1
and S2 as the output.

Input Format:
The first line contains S.
The second line contains L1.

Output Format:
The first line contains S1.
The second line contains S2.

Boundary Conditions:
4 <= LENGTH(S) <= 100
1 <= LENGTH(S1) <= 99
1 <= LENGTH(S2) <= 99

Example Input/Output 1:

Input:
LBARZIYSK
4

Output:
LAZY
BRISK
